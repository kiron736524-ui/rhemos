---
rubric: inspection
用途: 生图质量的客观维度表。默认用于生成前自律(预防)；生成后判图/一致性检查仅在用户明确要求 AI 诊断、筛选或修正时启用。
source: 重组自旧 rhemax tasks/result-review.md + tasks/prompt-craft.md(编译检查) + nano/booth-fundamentals.md(self-check) + 已废弃 quality-check 的判图维度；2026-06 调整为"首稿候选由用户选择，AI 诊断按需启用"
---

# 生图检查 Rubric

> 旧 rhemax 把生成后视觉质检废弃了，根因是它把"半成品+报告"甩给用户、逼二次操作。当前体系**保留检查、改变默认值**：生成前预防是必做；生成后 AI 判图/一致性检查是按需工具，不再默认串进首稿链路。首稿候选先交给用户选择基准图。

## 三种用法

1. **生成前 · prompt 纪律清单（主力）**：写 prompt 时逐维度自查，预防胜于纠正。零延迟、零打扰。这是质量的主要来源。
2. **生成后 · 用户选择优先**：默认首稿是两张 candidate-set，交给用户选基准图；不要默认用 AI 判图替用户做主观选择。
3. **按需诊断 / 修复**：只有用户明确要求 AI 帮忙诊断、筛选、一致性检查或修正时，才启用 `autoCheck` 或 `revise_asset`。**warning / 口味级不自动修。**

判图永远是 **"输出 vs DesignSpec" 的客观对比**，返回 `{维度, pass/warning/fail, evidence}`。如果用户要求诊断，可以用自然语言给出简短建议；如果只是常规首稿，不要输出质检报告。

## 客观 vs 口味（红线）
- **客观缺陷**（下列维度）→ 生成前预防；生成后仅在用户要求诊断/修正时处理。
- **主观口味**（"想更暖/更大气/换个色"）→ 不是失败，是正常设计对话，等用户自然提或让用户从候选中选择。
- 判级原则：可落地性 > 动线与功能 > 品牌表达 > 风格美感。明显结构错误绝不写成 pass；信息不足以判断时标 warning（建议生成多视角/平面验证），不假装确定。

## 维度（均为客观可判项）

### A. 结构与物理可信（最高优先，fail 级）
1. 无悬浮结构；屏/桁架/顶棚/吊牌都有物理支撑。
2. 无穿插、无裁切主结构、无比例失真。
3. 跨度：木≤6m、桁架≤9m 无中支撑；未知吊点不画成吊装。
4. LED/大屏有承重墙体+维护空间；电子区有地台走线。

### B. 空间与动线
5. 开口方式/背墙方位/主通道方向正确。
6. 入口-接待-主视觉-产品-洽谈-储物 可走通、无死角拥堵。
7. 会议室尺度与容量匹配；贴主墙时齐高。
8. 产品有清晰载体/尺度/观看距离/演示空间，未被遮挡。

### C. 一致性（多视图专用）
9. 跨视角 结构/尺度/材质/色彩/灯光 统一（同材质表、同色温、同灯光氛围）。
10. 品牌位置跨视角一致。
11. 视角确实变了（非主图近似复制/裁切/微调）；侧视图侧立面主导画面。
12. 俯视图是正交坐标图而非美化鸟瞰；canopy/truss 在俯视图中半透明不挡内部。

### D. 材质/灯光/品牌/渲染纯净度
13. 材质是真实展览材料 + 带表面处理；灯具有真实来源、基础/重点/氛围三层、色温明确。
14. 品牌识别朝主通道、主次清晰、占位而非乱码、logo 不失真。
15. 无文字乱码、无重复屏幕、无人物喧宾夺主、背景不喧闹、photorealistic 商业可信。
16. 输出尺寸/宽高比与请求一致（编辑场景尤其：不裁/不缩/不变比例）。

## 常见硬伤补充（实战"禁止模式"，并入客观判级）

除上述维度，下列是反复出现的客观错误，发现即按 fail/warning 处理：
- 薄墙挂大型主 LED 屏（大屏必须厚墙承载 ~80cm，背后可检修/进人）。
- LED 墙背部无检修/进人空间。
- 大件展品（冰箱/洗衣机/摩托/设备）放在高柜上（应落地或 10-30cm 小地台）。
- 家具/绿植过多，挤占主展示区（配套不能喧宾夺主）。
- 墙体/柜体/屏幕/顶部之间没有厚度与嵌套关系，像平面拼贴。
- 只有家具和画面、没有完整展台结构（六大系统呈现不足，像空场景/临时布置）。
- 无顶部方案却没有门头/墙体体量/立面造型补偿（变成空盒子）。
- 洽谈区围合过低（<2.5m），像简易隔断。
- 小展台夸张超高、比例失真。

## 修复纪律（用户要求时）
- 只针对 fail 级客观硬伤；**写定向纠正 prompt**，从对的部分采样（如"logo 字体取自前视图"），用 gpt-image-2 编辑模式做定点修，不从头重生。
- 改完是否再诊断由用户目标决定；不要因为一次失败自动扩大成多轮重试。
- 遇到**必须用户拍板的真实抉择**（如尺寸放不下全部功能区）才用卡片选项让用户决定。

## 与"预防"的衔接
用法 1（生成前清单）= 旧团队"自检前移"的硬道理，是主力；用法 2/3（生成后诊断+修复）= 用户明确需要时的工具，不是默认链路。
